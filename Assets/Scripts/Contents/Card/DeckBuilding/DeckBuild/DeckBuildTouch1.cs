using UnityEngine;

public class DeckBuildTouch1 : MonoBehaviour
{
    float[] touchTimer = new float[5];
    bool movementMode;
    private void Update()
    {
        // 터치 감지
        // 시간 감지 후 짧은 시간 안에 터치가 끝났다면 CardInfo 참조
        
        // 카드 혹은 카드가 있는 슬롯에 오래 터치 시 카드 이동

        // 이동모드가 아닐 때, 손가락 움직이는 위치가 deck에 있다면 deck을 이동시킴
    }
}
