using UnityEngine;

public class ManipulableCard : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    
    // 확대
    // 카드 일람에서는 클릭만 해도 확대
    // 덱 빌딩, 가루 추출 시 오랫동안 클릭했을 때 확대

    // 끌고 가기 (덱 빌딩, 가루 추출)
    // 카드 클릭 후 특정 거리 이상 드래그하면 활성화하여 손가락을 따라다님
    // 위치가 덱 빌딩 혹은 가루 추출 칸이면 그 칸으로 이동, 아니라면 원위치로 이동




}
